[project]
name = "sleeping-bird-api"
version = "1.0.0"
description = "FastAPI backend for Sleeping Bird — Gemini-powered metric suggestions."
requires-python = ">=3.11"
dependencies = [
    "uvicorn[standard]>=0.32",
    "pydantic>=2.9",
    "pydantic-settings>=2.6",
    "google-genai>=0.8",
    "fastapi>=0.136.0",
]

[tool.ruff]
line-length = 100
target-version = "py311"
